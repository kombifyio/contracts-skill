## Contracts Skill

Before changing code, run a contract preflight: find the nearest `CONTRACT.md`, read it with `CONTRACT.yaml`, compare `meta.source_hash`, and summarize relevant MUST/MUST NOT constraints. If drift exists, sync `CONTRACT.yaml` before implementation. Treat `CONTRACT.md` as user-owned and do not edit it unless the user asks for a draft or approves the change.
